# Nima Game

Minimal scaffold for client (Phaser + Vite) and server (Colyseus).

## Client
- Install: `cd client && npm install`
- Dev: `npm run dev`

## Server
- Install: `cd server && npm install`
- Dev: `npm run dev`
